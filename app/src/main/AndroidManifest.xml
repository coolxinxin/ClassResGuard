<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.leos.superplugin">

    <application
        android:name="com.leos.superplugin.leoapp.LeoLeoLargeMyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SuperPlugin"
        tools:targetApi="31">
        <activity
            android:name="com.leos.superplugin.leoactivity.LeoLeoLargeLoginActivity"
            android:exported="false" />
        <activity
            android:name="com.leos.superplugin.leoactivity.LeoLeoLargeMainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>

    </application>

</manifest>
